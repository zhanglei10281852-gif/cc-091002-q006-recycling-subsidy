export class SubsidyService { constructor(store,rates){this.store=store;this.rates=rates;} add(item){this.store.add(item);} settle(company,month,capCents){let used=0;const lines=this.store.transactions.filter(x=>x.company===company&&x.occurredAt.startsWith(month)).map(item=>{const rate=this.rates.current(item.material);const cents=Math.round(item.weightKg*rate.centsPerKg);const granted=Math.max(0,Math.min(cents,capCents-used));used+=granted;return {...item,rateVersion:rate.version,grantedCents:granted};});const batch={company,month,totalCents:lines.reduce((n,x)=>n+x.grantedCents,0),lines};this.store.saveBatch(`${company}:${month}`,batch);return batch;} }
