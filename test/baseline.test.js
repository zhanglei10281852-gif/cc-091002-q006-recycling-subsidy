import test from 'node:test'; import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises'; import { RateBook } from '../src/rate-book.js'; import { SubsidyStore } from '../src/subsidy-store.js'; import { SubsidyService } from '../src/subsidy-service.js';
test('整公斤交易按当前费率结算',async()=>{const data=JSON.parse(await readFile(new URL('../fixtures/settlement.json',import.meta.url)));const service=new SubsidyService(new SubsidyStore(),new RateBook(data.rates));service.add(data.transaction);const batch=service.settle('REC-1','2026-09',data.monthlyCapCents);assert.equal(batch.totalCents,2500);assert.equal(batch.lines[0].rateVersion,'2026.1');});
